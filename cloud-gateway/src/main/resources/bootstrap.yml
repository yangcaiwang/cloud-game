# Tomcat
server:
  port: 9000
# Spring
spring:
  application:
    # 应用名称
    name: cloud-gateway
  profiles:
    # 环境配置
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 43.138.76.94:8848
      config:
        server-addr: 43.138.76.94:8848
        file-extension: yml