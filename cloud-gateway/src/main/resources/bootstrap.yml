# Tomcat
server:
  port: 9000
# Spring
spring:
  application:
    # 应用名称
    name: cloud-gateway
  profiles:
    # 环境配置
    active: dev
  cloud:
    # k8s 内部nacos服务
    nacos:
      discovery:
        server-addr: 120.46.209.80:30250
      config:
        server-addr: 120.46.209.80:30250
        file-extension: yml
