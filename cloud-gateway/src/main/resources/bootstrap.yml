# Tomcat
server:
  port: 9000
# Spring
spring:
  application:
    # 应用名称
    name: cloud-gateway
  profiles:
    # 环境配置
    active: dev
  cloud:
    # k8s 内部nacos服务
    nacos:
      discovery:
        server-addr: 10.10.23.204:8848
      config:
        server-addr: 10.10.23.204:8848
        file-extension: yml
